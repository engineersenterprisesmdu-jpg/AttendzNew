/**
 * HARDCODED FIREBASE CONFIGURATION
 * 
 * To bundle your Firebase configuration with the deployment (so that ALL browsers
 * and employee devices connect to the same cloud database automatically without 
 * requiring manual setup in their local browser storage):
 * 
 * 1. Fill in the keys below with your values from your Firebase Project Console.
 * 2. Deploy or build the app (e.g. push to GitHub Pages).
 */
export const HARDCODED_FIREBASE_CONFIG = {
  // Replace these empty strings with your actual Firebase Project keys:
  apiKey: "",
  projectId: "",
  appId: "",
  databaseURL: "" // Option: Leave empty or match databaseURL if needed
};
